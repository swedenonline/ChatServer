# /web/index.html

Before running project, Make sure to change below javascript variables.


//Web Socket Url to server...
var socketUrl = "ws://localhost:8080/ChatServer/start";


// For testing purposes, userId and userName are set to static.
var userId   = Math.floor((Math.random() * 10) + 1);
var userName = "User"+userId;


//adjust online users div height
var userListHeight = screen.height;


//This variable enables/disables public chat...
var allowPublicChat = true;
